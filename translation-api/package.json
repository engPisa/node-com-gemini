{
  "name": "translation-api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "clean": "rm -rf node_modules package-lock.json && npm install",
    "generate": "prisma generate",
    "dev": "npm run generate && node --watch src/server.js",
    "prod": "npm ru generate && node src/server.js"
  },
  "dependencies": {
    "@prisma/client": "^5.15.0",
    "amqplib": "^0.10.4",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "prisma": "^5.15.0"
  }
}